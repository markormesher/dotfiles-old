alias v='nvim'
alias vim='nvim'

function vw {
  old_path="$(pwd)"
  cd ~/vimwiki
  nvim +:VimwikiIndex
  cd "$old_path"
}

function vd {
  old_path="$(pwd)"
  cd ~/vimwiki
  nvim +:VimwikiMakeDiaryNote
  cd "$old_path"
}
